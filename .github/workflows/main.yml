name: 天翼云盘签到

on:
  # 手动运行
  workflow_dispatch:
  # 定时运行：UTC 1:30 = 北京时间 09:30，每天一次
  schedule:
    - cron: "30 1 * * *"
    - cron: "30 12 * * *"

jobs:
  checkin:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉取仓库代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 安装 Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3. 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. 运行签到脚本，输出到 index.md
      - name: Run check-in script
        id: checkin
        env:
          TYYP_USERNAME: ${{ secrets.TYYP_USERNAME }}
          TYYP_PSW: ${{ secrets.TYYP_PSW }}
        run: |
          echo "开始天翼云盘签到..."
          # 仓库自带的 main.py 会根据 TYYP_USERNAME/TYYP_PSW 自动处理多账号和签到
          python main.py > index.md
          echo "签到完成，结果已写入 index.md"

      # 5. 提交 index.md（方便在仓库里查看签到记录）
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动签到更新"
          file_pattern: "index.md"
          skip_dirty_check: true

      # 6. PushPlus 推送提醒（成功或失败都会发）
      - name: Push notification (PushPlus)
        if: always()
        uses: dext7r/pushNotifications@v1.0.3
        with:
          TYPE: PushPlus
          TITLE: "天翼云盘签到结果"
          DESP: |
            本次签到状态：${{ job.status }}
            触发方式：${{ github.event_name }}（workflow_dispatch=手动，schedule=定时）
            运行分支：${{ github.ref_name }}
            仓库：${{ github.repository }}

            运行日志地址：
            https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            详细签到信息会写入本仓库根目录下的 index.md 文件，可在 GitHub 上查看。

            **签到结果：**
            账号 138xxxx1234  
            ✔ 签到成功  
            ⏱ 时间：08:27  
            📦 获得空间：+59M  

            **抽奖结果：**  
            ❌ 第1次：抽奖失败，次数不足  
            ❌ 第2次：抽奖失败，次数不足  
            ❌ 第3次：抽奖失败，次数不足
          PUSH_PLUS_TOKEN: ${{ secrets.PUSH_PLUS_TOKEN }}
